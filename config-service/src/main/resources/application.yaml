spring:
  application:
    name: config-server

  cloud:
    config.server.git:
      uri: https://github.com/DhruvGithub414/websmith-config-server
      username: DhruvGithub414
      password: ${PAT}
      default-label: main

server.port: 8888